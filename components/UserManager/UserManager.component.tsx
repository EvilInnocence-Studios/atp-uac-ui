import {UserManagerProps} from "./UserManager.d";
import styles from './UserManager.module.scss';

export const UserManagerComponent = ({}:UserManagerProps) =>
    <div>UserManager component goes here.</div>;
